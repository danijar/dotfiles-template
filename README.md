# Dotfiles Template

## Vim Bindings

To enable Vim bindings, comment in the following settings:

- The entire file `.ipython/profile_default/ipython_config.py`
- The entire file `.inputrc`
- The line `bindkey -v` in `.zshrc`
